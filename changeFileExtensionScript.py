"""
Author: Thanos Moschou
Description: I needed to change the .txt extension to .java or .sql so I created this script
"""

import os

# pathForJava = "CodingProblems/"
# txtFiles = [f for f in os.listdir(pathForJava) if f.endswith(".txt")]

# for txt in txtFiles:
#     #Create a new file with .java extension
#     new_name = os.path.split(txt)[1].split(sep = ".")[0] + '.java'
#     #Rename the file
#     os.rename(os.path.join(pathForJava, txt), os.path.join(pathForJava, new_name))

# pathForSql = "SQL50/"
# txtFiles = [f for f in os.listdir(pathForSql) if f.endswith(".txt")]

# for txt in txtFiles:
#     #Create a new file with .sql extension
#     new_name = os.path.split(txt)[1].split(sep = ".")[0] + '.sql'
#     #Rename the file
#     os.rename(os.path.join(pathForSql, txt), os.path.join(pathForSql, new_name))

# print("Complete")
